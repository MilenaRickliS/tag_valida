import 'package:flutter/material.dart';

class EtiquetaAtivaDetalhes extends StatelessWidget {
  const EtiquetaAtivaDetalhes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: const Center(
          child: Text(
            'Page Etiqueta Ativa Detalhes',
            style: TextStyle(fontSize: 32),
          ),
        ),
    );
  }
}
