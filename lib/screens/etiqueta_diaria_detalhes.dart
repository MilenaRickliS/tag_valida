import 'package:flutter/material.dart';

class EtiquetaDiariaDetalhesScreen extends StatelessWidget {
  const EtiquetaDiariaDetalhesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: const Center(
          child: Text(
            'Page Etiqueta Diaria Detalhes',
            style: TextStyle(fontSize: 32),
          ),
        ),
    );
  }
}
